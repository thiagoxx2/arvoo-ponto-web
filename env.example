# Supabase Configuration
# Copie este arquivo para .env e configure com suas credenciais reais

# URL do seu projeto Supabase (encontre em Settings > API)
VITE_SUPABASE_URL=https://seu-projeto-id.supabase.co

# Chave anônima do Supabase (encontre em Settings > API)
VITE_SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# Storage Configuration
# Nome do bucket usado para armazenar fotos de pontos (padrão: fotos-pontos)
VITE_FOTOS_BUCKET=fotos-pontos

# Bucket privado? (true = usar signed URLs, false = URLs públicas)
# Obs: signed URLs serão implementadas na Iteração 3
VITE_FOTOS_BUCKET_PRIVATE=false

# TTL (em segundos) para signed URLs (padrão: 900 = 15 minutos)
VITE_FOTOS_TTL_SECONDS=900

# Debug & Diagnostics
# Habilita logs detalhados de diagnóstico (1 = ativo, 0 ou ausente = inativo)
VITE_DIAGNOSTICS=0
